# NNNN. <decision title — one line, imperative: e.g. "load context via resources, not hooks">

- **Status**: proposed
- **Date**: YYYY-MM-DD
- **Layer**: framework | task:<task-name>

## Context

The problem, constraints, and forces at play when the decision was made. Why a decision was needed at all.

## Decision

What was chosen. Active voice: "We load context via `resources`."

## Consequences

Results and costs of this decision: what got easier, what got harder, what known traps it leaves behind (link to RESUME next-steps / CR rows if relevant).

## Alternatives considered

- **Option A** — why rejected
- **Option B** — why rejected

(This is the important section: record the paths considered-but-not-taken, and why, so a future reader doesn't re-walk them.)
