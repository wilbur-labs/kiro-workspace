# Vision: {{TASK_NAME}}

> Fill in each section before kicking off AI-DLC. Delete the inline hints
> (lines prefixed with `>`) once the section has real content. See
> `.kiro/templates/inputs/vision-document-guide.md` for the long-form guide
> and `example-*-vision-*.md` for worked examples.

## Executive Summary

> 3-5 sentences. Anyone reading only this section should understand WHAT the
> project is, WHO it serves, and WHY it exists.

<project-name> is a <type of system/product> that enables <target users> to <core capability>.
It addresses <business problem or opportunity> by <approach or differentiation>.
The expected outcome is <measurable business result>.

## Features In Scope (MVP)

> Bullet list. Be specific enough that "done" is unambiguous. Avoid adjectives
> like "fast" or "robust" — quantify in the success-metrics section instead.

- <feature 1>
- <feature 2>
- <feature 3>

## Features Explicitly Out of Scope (MVP)

> Just as important as in-scope. Naming what is NOT in MVP is the strongest
> defense against scope creep. Mark when each item is planned (Phase 2 / Phase 3
> / "no plan").

- <out-of-scope item> (Phase 2)
- <out-of-scope item> (Phase 3)
- <out-of-scope item> (no plan)

## Target Users

> Who will use this? Be specific. "Developers" is too broad — say which
> developers, in which context, with which alternative they would otherwise use.

- <user segment 1>
- <user segment 2>

## Key Success Metrics

> Quantified, measurable, time-bounded. Avoid "improve" or "increase" without
> a baseline + target.

- <metric 1, e.g. "1,000 registered accounts within 3 months">
- <metric 2, e.g. "p50 response time < 50ms">
- <metric 3, e.g. "zero critical accuracy regressions">

## Open Questions

> Anything blocking a clean MVP definition. Empty is fine — but if you're
> unsure about a design or constraint, raise it here BEFORE inception starts.

- <open question 1>
- <open question 2>
