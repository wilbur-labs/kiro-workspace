# Learned — dogfood-todo-api

Project-specific learnings for **this task only**. Loaded into agent at spawn.

**What belongs here** (project-specific, won't generalize):
- This project's schema quirks, PK choices, table naming
- Domain rules and business logic surprises
- Data shape gotchas (`null` semantics, encoding, units)
- This codebase's local conventions that differ from defaults

**What belongs in `.kiro/learned/LEARNED.md` instead** (cross-task reusable):
- Tool / framework / language gotchas
- Internal-system integration recipes (SSO, internal APIs, package registries)
- Snippets you'd reach for in a different project

See `.kiro/skills/memory-layering.md` for the decision tree. See `.kiro/skills/auto-learn.md` for capture rules.

---

## Entries

<!--
Format:

### [YYYY-MM-DD] AgentName — Short title

Description (1-3 lines, key info only).

-->
