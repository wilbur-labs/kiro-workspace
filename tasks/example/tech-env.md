# Technical Environment (placeholder)

> Copy from `.kiro/templates/inputs/example-minimal-tech-env-*.md` and fill in your project details.
> This file feeds into AI-DLC to reduce clarifying questions.

## Language & Framework

- Language: (e.g. TypeScript 5.x)
- Framework: (e.g. Express, Next.js)
- Package manager: (e.g. pnpm)

## Deployment

- Cloud: (e.g. AWS)
- Model: (e.g. Lambda + API Gateway, ECS, etc.)

## Testing

- Framework: (e.g. vitest, pytest)

## Prohibited Libraries

| Library | Reason | Alternative |
|---------|--------|-------------|
| (example) | (why not) | (use this instead) |

## Example Code Patterns

> Include at least one example each for: typical endpoint, function, test.
> These are the highest-leverage addition — AI-DLC uses them as code generation patterns.

```
// TODO: paste a representative code snippet from your project
```
